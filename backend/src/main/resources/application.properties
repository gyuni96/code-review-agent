spring.application.name=code-review-agent

# 외부 설정 파일들을 임포트하여 구조화
spring.config.import=optional:application-db.properties, \
                     optional:application-redis.properties, \
                     optional:application-logging.properties

# 기본 활성 프로파일 설정
spring.profiles.active=dev
